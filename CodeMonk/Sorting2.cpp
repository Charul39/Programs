#include<bits/stdc++.h>
#define lld long long int
using namespace std;

int main()
{
  string s;
  lld k;
  cin >> s >> k;
  
  char x='a';
  for(i=0;i<s.size();i++)
  {
    if(s[i])
  }
  return 0;
}
